<nav class="navbar navbar-expand-lg navbar-light bg-primary">
    <a class="navbar-brand" href="{{ path('game') }}">Pixel Souls</a>
    <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarColor03" aria-controls="navbarColor03" aria-expanded="false" aria-label="Toggle navigation">
        <span class="navbar-toggler-icon"></span>
    </button>

    <div class="collapse navbar-collapse" id="navbarColor03">
        <ul class="navbar-nav mr-auto">
            <li class="nav-item">
                <a class="nav-link" href="#"><i class="fas fa-chart-line"></i>&nbsp;Dashboard</a>
            </li>
            <li class="nav-item">
                <a class="nav-link" href="{{ path('admin_users_index') }}"><i class="fas fa-users"></i>&nbsp;Users</a>
            </li>
            <li class="nav-item">
                <a class="nav-link" href="{{ path('admin_characters_index') }}"><i class="fas fa-hand-rock"></i>&nbsp;Characters</a>
            </li>
            <li class="nav-item">
                <a class="nav-link" href="{{ path('admin_posts_index') }}"><i class="fas fa-folder"></i>&nbsp;Posts</a>
            </li>
            <li class="nav-item">
                <a class="nav-link" href="{{ path('admin_comments_index') }}"><i class="fas fa-comment"></i>&nbsp;Comments</a>
            </li>
        </ul>
        <ul class="navbar-nav ml-auto">
            <li class="nav-item">
                <a href="{{ path('game') }}" target="_blank" class="nav-link">
                    <i class="far fa-eye"></i>
                    Check website
                </a>
            </li>
        {% if app.user %}
            <li class="nav-item dropdown">
                <a href="#" class="nav-link dropdown-toggle" data-toggle="dropdown" id="accountDropDownLink">
                    <img src="/img/avatars/{{ app.user.image }}" class="avatar avatar-mini mr-2" alt="Avatar de {{ app.user.fullName }}">
                    {{ app.user.pseudo }}
                </a>
                <div class="dropdown-menu dropdown-menu-right text-center" aria-labelledby="accountDropDownLink">
                    <a href="{{ path('admin_account_logout') }}" class="dropdown-item">Logout</a>
                </div>
            </li>
        {% endif %}
        </ul>
    </div>
</nav>

{% for label, messages in app.flashes %}
    <div class="container">
        <div class="alert alert-{{ label }} text-center">
            {% for message in messages %}
                <p>{{ message | raw }}</p>
            {% endfor %}
        </div>
    </div>
{% endfor %}